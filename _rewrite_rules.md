# 기존 문항 재작성(난이도 강화) 규칙

## 출력 형식
`/tmp/rewrite/subjectN_fix.js` 파일에 아래 형태로 작성:

```js
module.exports = [
{id:1003,s:1,k:"1-1-2",t:"개발 모형",f:"A",q:"...",c:["...","...","...","..."],a:2,e:"..."},
{id:1006,s:1,k:"1-1-2",t:"소프트웨어 공학 기초",f:"C",q:"...",c:[...],a:1,e:"..."}
];
```

- **id는 절대 바꾸지 마세요.** 학습 기록이 id 기준으로 유지됩니다.
- `s`(과목 번호)도 바꾸지 마세요.
- `k`(출제기준 코드)를 **새로 부여**하세요. `_kmap.md` 표의 값만 사용.
- `t`(토픽), `f`(빈도등급)는 필요하면 조정 가능합니다.
- `q`, `c`, `a`, `e`는 자유롭게 재작성하세요.

## 재작성 목표
지정된 문항들은 **지식이 없어도 소거법·패턴으로 풀리는** 결함이 있습니다.
아래 4가지 방향 중 문항에 맞는 것으로 전환하세요.

1. **사례 적용형** — 상황을 서술하고 해당 개념을 판정하게 함
   예) "정렬 방식을 런타임에 교체해야 한다" → Strategy 패턴
   예) "장애 발생 후 30초 이내 서비스 복구" → 가용성
2. **추적·계산형** — 코드 출력, 결과 행 수, 부재 횟수, 이동거리, 경로 길이
3. **부정형·비교 판별형** — "옳지 않은 것", "잘못 짝지어진 것"
   (실제 시험의 부정형 비율은 40~65%인데 현재 문제은행은 22%)
4. **순서 배열형** — 프로세스·절차의 순서

## 절대 금지 (이번 감사에서 적발된 패턴)
- ✗ 개발자 개인 신상형 오답: "개발자의 개인 일정", "개발자 연봉", "개발자 학력 사항"
- ✗ 존재하지 않는 조어: "비용 뷰", "정규 분할(Normal)", "저렴(Cheap)"
- ✗ 존재하지 않는 구문: `WHERE 점수 == NULL`, `MODIFY 학생 SET`
- ✗ 정답의 반의어를 오답으로 배치
- ✗ 범주가 다른 오답 (보안 도구를 묻는데 스케줄러를 오답으로)
- ✗ 지문에 정답 키워드 노출 ("비동기로" → AJAX, "소프트웨어 정의" → SDN)
- ✗ 정답 보기만 유난히 긴 경우

## 오답 보기 구성 원칙
**보기 4개를 모두 같은 계열·같은 층위의 실재하는 개념**으로 구성하세요.
정확히 알아야만 구분되도록 만드는 것이 목표입니다.

## 해설(e) 기준
- **최소 60자 이상**
- **오답 보기 중 최소 1개가 왜 틀렸는지 반드시 설명**
- 계산·추적형은 단계별 서술 포함

## 정답 인덱스
재작성 대상 전체의 정답 인덱스를 0/1/2/3 각 25% 내외로 배분하세요.
(현재 전체 문제은행은 0번에 44% 몰려 있습니다)

## 해설 누출 금지
해설에 **다른 문항의 정답이 되는 정보를 나열하지 마세요.**
예) 응집도 문항의 해설에 "기순교절시논우" 전체 서열을 쓰면 다른 응집도 문항이 자동으로 풀립니다.

## 사실 정확성
- 확인된 사실만. 불확실하면 원 문항의 정답을 유지하고 보기·해설만 개선하세요.
- 코드·계산 문항은 **반드시 실제 실행하거나 계산해서 검증**하세요.

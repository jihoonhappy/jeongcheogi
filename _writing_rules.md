# 신규 문항 작성 규칙 (공통)

## 문항 객체 형식
```js
{id:1101,s:1,k:"1-1-1",t:"현행 시스템 분석",f:"B",q:"지문",c:["보기1","보기2","보기3","보기4"],a:0,e:"해설"}
```
- `id`: 과목별 대역. 1과목 1101~, 2과목 2101~, 3과목 3101~, 4과목 4101~, 5과목 5101~ (배정 범위 엄수)
- `s`: 과목 번호 (1~5)
- `k`: 출제기준 코드 — `_kmap.md` 표에 있는 값만 사용. **필수**
- `t`: 토픽명 (약점 분석 단위). 한글 우선, 필요 시 괄호로 영문 병기
- `f`: 빈도등급 A(최다출제)/B(자주)/C(가끔)
- `q`: 지문. 줄바꿈은 `\n`
- `c`: 보기 정확히 4개
- `a`: 정답 인덱스 0~3
- `e`: 해설

## 출력 파일 형식
파일 전체를 아래 형태로 작성:
```js
/* N과목 · <과목명> P1 신규 문항 */
addQ([

{id:...},
{id:...}

]);
```

---

## 품질 규칙 (반드시 준수 — 이번 감사에서 지적된 결함의 재발 방지)

### 1. 정답 인덱스를 고르게 분산할 것
현재 문제은행은 정답이 0번에 48.6% 몰려 있습니다.
**작성하는 문항의 정답 인덱스 0/1/2/3을 각각 25% 내외로 배분하세요.**

### 2. 오답 보기를 "농담"으로 만들지 말 것 — 가장 중요
다음은 이번 감사에서 실제로 적발된 나쁜 패턴입니다. 절대 쓰지 마세요.
- ✗ 개발자 개인 신상형: "개발자의 개인 일정", "개발자 연봉", "개발자 학력 사항"
- ✗ 존재하지 않는 조어: "비용 뷰", "정규 분할(Normal)", "저렴(Cheap)"
- ✗ 존재하지 않는 구문: `WHERE 점수 == NULL`, `MODIFY 학생 SET`, `CREATE TABLE 학생 ADD`
- ✗ 정답의 반의어를 오답으로: 정답이 "명확성"인데 오답이 "모호성"
- ✗ 범주가 다른 오답: 보안 도구를 묻는데 오답에 스케줄러(cron)를 배치
- ✗ 정답 보기만 눈에 띄게 긴 경우 (보기 길이를 비슷하게 맞출 것)

**올바른 방법**: 오답 4개를 모두 **같은 계열·같은 층위의 실재하는 개념**으로 구성하고,
정확히 알아야만 구분되도록 만드세요.

### 3. 지문에 정답을 노출하지 말 것
- ✗ "**비동기**로 주고받는 기술은?" → 정답 AJAX(Asynchronous)
- ✗ "**소프트웨어 정의** 네트워크에서…" → 정답 SDN
- ✗ "**견본품**을 미리 만들어…" → 정답 프로토타입

### 4. 해설(e) 작성 기준
- **최소 60자 이상**
- **오답 보기 중 최소 1개가 왜 틀렸는지 반드시 설명**
- 코드·계산 문항은 한 단계씩 추적하는 서술을 포함할 것

### 5. 유형을 다양화할 것
전체를 "용어 → 정의 1:1 매칭"으로 채우지 마세요. 최소 절반은 아래 유형으로:
- **사례 적용형**: 상황을 서술하고 해당 개념을 고르게 함
- **추적·계산형**: 코드 출력, SQL 결과 행 수, 페이지 부재 횟수, 헤드 이동거리, 임계경로 등
- **부정형·비교 판별형**: "옳지 않은 것", "잘못 짝지어진 것" (실제 시험의 부정형 비율은 40~65%)
- **순서 배열형**: 프로세스·절차의 순서

### 6. 사실 정확성
- 확인된 사실만 쓸 것. 불확실하면 그 문항을 만들지 말 것.
- 표준·제원(키 길이, 라운드 수, 표준 번호, 연도)은 반드시 1차 출처로 확인할 것.
- 코드·계산 문항은 **반드시 실제로 실행하거나 계산해서 정답을 확인**할 것.

### 7. 기존 문항과 중복 금지
같은 과목 파일(`data/subjectN.js`)의 기존 100문항을 먼저 훑어보고,
같은 지식을 반복하는 문항을 만들지 마세요. 해설이 다른 문항의 정답을 미리
알려주는 것도 금지입니다.

### 8. 실기 자료 혼입 금지
필기는 **4지선다**입니다. SQL·코드를 직접 작성하게 하는 문항은 만들 수 없습니다.
결과 예측형·오류 판별형·순서 판별형으로 구현하세요.
인터넷의 "자료구조 킬러문항", "9문제 출제" 같은 서술은 전부 실기 자료이므로 참고하지 마세요.
